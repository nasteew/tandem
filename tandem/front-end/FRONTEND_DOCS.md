# Документация Фронтенда и Архитектура

## 📂 Структура Проекта (Project Structure)

В проекте используется масштабируемая архитектура, разделенная на слои (Данные, Логика, Вид).

### 1. `api/` (Взаимодействие с сервером)

- **Для чего:** Слой работы с сетью.
- **Что внутри:**
  - Конфигурация HTTP клиента (обычно Axios).
  - Функции-запросы к бэкенду (например, `authApi.login`, `usersApi.getProfile`).

### 2. `assets/` (Ресурсы)

- **Для чего:** Статические файлы.
- **Что внутри:** Изображения, глобальные стили, шрифты, SVG иконки.

### 3. `components/` (Компоненты)

- **Для чего:** Переиспользуемые элементы интерфейса.
- **Структура:**
  - `ui/` — Базовые ("глупые") компоненты: Кнопки, Инпуты, Чекбоксы.
  - `shared/` — Более сложные блоки: Хедер, Футер, Карточка товара.

### 4. `constants/` (Константы)

- **Для чего:** Данные, которые не меняются.
- **Что внутри:** Настройки навигационного меню, списки опций для селектов, конфигурационные переменные.

### 5. `hooks/` (Хуки)

- **Для чего:** Переиспользуемая логика React.
- **Примеры:** `useAuth` (проверка авторизации), `useTheme` (смена темы), `useDebounce` (оптимизация поиска).

### 6. `layout/` (Макеты)

- **Для чего:** Обертки страниц (Лейауты).
- **Примеры:**
  - `MainLayout` — Сайдбар + Хедер (для дашборда).
  - `AuthLayout` — Центрированный контейнер (для входа/регистрации).

### 7. `pages/` (Страницы)

- **Для чего:** Основные экраны приложения (то, что видит пользователь по конкретному адресу).
- **Состав:** `LandingPage`, `DashboardPage`, `AuthPage`, `LibraryPage` и др.

### 8. `provider/` (Провайдеры)

- **Для чего:** Глобальные настройки для всего приложения (Context API).
- **Примеры:** `AuthProvider` (хранение сессии), `ThemeProvider`, `Redux/QueryProvider`.

### 9. `router/` (Роутинг)

- **Для чего:** Навигация.
- **Что внутри:** Файл настройки маршрутов (`react-router`), связывающий URL с компонентами из папки `pages`.

### 10. `schema/` (Валидация)

- **Для чего:** Схемы проверки данных.
- **Что внутри:** Zod схемы для форм (проверка email, пароля при регистрации).

### 11. `store/` (Стейт-менеджмент)

- **Для чего:** Глобальное состояние приложения.
- **Что внутри:** Zustand сторы (хранение данных пользователя, настроек интерфейса).

### 12. `types/` (Типы)

- **Для чего:** TypeScript интерфейсы.
- **Что внутри:** Описания сущностей (`User`, `Course`, `Message`), используемые во всем проекте.

### 13. `utils/` (Утилиты)

- **Для чего:** Вспомогательные функции.
- **Что внутри:** Форматирование дат, обработка строк, функции-хелперы.

---

## 📚 Библиотеки (Libraries)

### Основные

- **`react`, `react-dom`**: Ядро UI.
- **`vite`**: Сборщик (быстрый старт и HMR).
- **`typescript`**: Статическая типизация.

### Данные и Состояние

- **`@tanstack/react-query`**: Синхронизация с сервером (кэширование, загрузка, ошибки). Заменяет `useEffect` для запросов.
- **`zustand`** (Рекомендуется): Легкий стейт-менеджер для клиентских данных.

### Формы

- **`react-hook-form`**: Работа с формами (производительность, простота).
- **`zod`**: Валидация данных (интегрируется с react-hook-form).

### UI и Стили

- **`lucide-react`**: Набор иконок.

### Специальные

- **`@monaco-editor/react`**: Редактор кода (как VS Code) для тренировок.
- **`date-fns`**: Работа с датами.
- **`react-markdown`**: Рендеринг ответов AI агента.
