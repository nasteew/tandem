# Tandem Backend Documentation

Этот документ содержит описание технологического стека, структуры проекта и инструкции по работе с backend-частью приложения Tandem.

## 🚀 Технологический стек

| Технология                                                           | Назначение                                                    |
| :------------------------------------------------------------------- | :------------------------------------------------------------ |
| **[NestJS v11](https://nestjs.com/)**                                | Основной Node.js фреймворк с модульной архитектурой.          |
| **[TypeScript](https://www.typescriptlang.org/)**                    | Строгая типизация для надежности кода.                        |
| **[Prisma v7](https://www.prisma.io/)**                              | Современная ORM для работы с базой данных.                    |
| **[PostgreSQL](https://www.postgresql.org/)**                        | Реляционная база данных.                                      |
| **[Swagger](https://swagger.io/)**                                   | Автоматическая документация API и песочница для тестирования. |
| **[Prettier](https://prettier.io/) / [ESLint](https://eslint.org/)** | Обеспечение единого стиля кода и поиск ошибок.                |

---

## 📁 Структура папок

- **`src/`** — Исходный код приложения.
  - **`main.ts`** — Точка входа в приложение. Здесь настраивается NestJS, Swagger и CORS.
  - **`app.module.ts`** — Глобальный модуль, где регистрируются все контроллеры и провайдеры.
  - **`app.controller.ts`** — Обработка входящих HTTP-запросов (маршруты).
  - **`app.service.ts`** — Бизнес-логика приложения.
  - **`prisma.service.ts`** — Инстанс Prisma Client для общения с базой данных.
  - **`dto/`** — Data Transfer Objects. Определяют форму данных, приходящих в запросах. Используются также для генерации Swagger-документации.
- **`prisma/`** — Конфигурация базы данных.
  - **`schema.prisma`** — Описание моделей данных (User и др.) и настроек подключения.
- **`generated/`** — Автоматически генерируемый код Prisma Client (типизация БД).

---

## 🛠 Ключевые скрипты

Выполняются из директории `back-end/`:

- `npm run start:dev` — Запуск сервера в режиме разработки с hot-reload.
- `npm run build` — Сборка проекта в JavaScript (папка `dist/`).
- `npm run lint` — Проверка кода линтером.
- `npm run format` — Автоматическое форматирование кода через Prettier.

### Работа с базой данных (Prisma):

- `npm run prisma:generate` — Генерация типов Prisma Client на основе `schema.prisma`.
- `npm run prisma:migrate` — Создание и применение миграций к БД.
- `npm run prisma:studio` — Запуск графического интерфейса для просмотра данных в БД.
- `npm run prisma:push` — Синхронизация схемы с БД без создания миграций (удобно для прототипирования).

---

## 📡 API и Swagger

Документация API доступна во время работы сервера по адресу:
`http://localhost:3001/api`

Здесь вы найдете все доступные эндпоинты, форматы запросов и ответов.

---

## ⚙️ Конфигурация (.env)

Для работы приложения необходим файл `.env` со следующими переменными:

- `PORT` — Порт, на котором будет запущен сервер (по умолчанию 3001).
- `DATABASE_URL` — Строка подключения к PostgreSQL.
  - _Пример:_ `postgresql://user:password@localhost:5432/tandem_db?schema=public`

---

## 💡 Основные концепции

1.  **Модульность**: Приложение разбито на логические блоки. Сейчас всё в `AppModule`, но по мере роста фич стоит выделять их в отдельные модули (например, `UserModule`, `AuthModule`).
2.  **DTO**: Все данные от клиента должны проходить через DTO. Это гарантирует, что мы получаем только то, что ожидаем.
3.  **DI (Dependency Injection)**: Сервисы помечаются декоратором `@Injectable()` и внедряются в контроллеры через конструктор. Это делает код тестируемым и гибким.
4.  **Driver Adapters**: Используется `@prisma/adapter-pg`, что позволяет использовать нативный драйвер `pg` для подключения, обеспечивая лучшую стабильность и производительность.
