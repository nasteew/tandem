# Дата: 2026-03-01

- **Что было сделано:**
1. Доработаны форма регистрации/авторизации и редирект.
Теперь процесс такой:
Пользователь вводит данные → валидация через Zod
Отправка формы → мутация TanStack Query
Успешный ответ → токен сохраняется в Zustand
Токен в store → доступен во всем приложении
Редирект → на защищенные страницы
При запросах → токен автоматически добавляется в заголовки
При выходе → токен удаляется из store
2. Доработан хедер в зависимости от авторизации пользователя

- **Проблемы:**
1. Не настроен профиль пользователя в зависимости от авторизации

- **Решения (или попытки):**
1. Дождаться готовности usera на беке и после этого настроить профиль в хедере.


- **Мысли / Планы:**

1. Использовать библиотеку для работы со временем
2. Вынести настройки танстака в отдельный файл.

- **Затраченное время:** 3 часа